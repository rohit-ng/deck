_format_version: "3.0"

services:
  - name: logger
    url: http://http-log.kong
    routes:
      - name: logger-home
        paths:
          - /logger
        path_handling: v1
        strip_path: true